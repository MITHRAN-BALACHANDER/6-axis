import React from 'react';

const Graph = () => {
    return (
        <div>
            <h1>Graph Page</h1>
            <p>This is where the graph will be displayed.</p>
        
        </div>
    );
};

export default Graph;